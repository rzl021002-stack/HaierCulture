# 快速起步指南

## 🚀 5 分钟内启动项目

### 前置条件
- Node.js 18.0+
- npm 或 yarn

### 步骤 1: 克隆项目
```bash
git clone https://github.com/rzl021002-stack/HaierCulture.git
cd HaierCulture
```

### 步骤 2: 安装依赖
```bash
npm install
```

### 步骤 3: 启动开发服务器
```bash
npm run dev
```

### 步骤 4: 打开浏览器
访问 [http://localhost:3000](http://localhost:3000)

---

## 📱 主要功能演示

### 1️⃣ 展厅总览 (3D 地图)
- 打开应用后默认显示
- 左侧是 3D 展厅地图
- 右侧是展区详情面板
- **操作：** 点击任何蓝色区域查看详情

### 2️⃣ 沉浸式漫游
- 点击"进入沉浸漫游"按钮
- 使用 WASD 或方向键移动
- 鼠标控制视角
- **操作：** 按 ESC 或点击"退出"返回

### 3️⃣ 文化值测评
- 切换到"文化测评"标签
- 依次回答 6 个问题
- 每题选 1-5 分
- **输出：** 自动生成文化画像报告

### 4️⃣ 案例库搜索
- 切换到"案例库"标签
- 输入关键词或选择阶段
- 即时显示相关案例
- **操作：** 点击卡片查看详情

---

## 🛠️ 常见命令

| 命令 | 说明 |
|------|------|
| `npm run dev` | 启动开发服务器 |
| `npm run build` | 构建生产版本 |
| `npm start` | 启动生产服务器 |
| `npm run type-check` | TypeScript 类型检查 |
| `npm run lint` | 代码 Lint 检查 |

---

## 📁 项目文件说明

### 重要文件位置

```
app/
  ├── page.tsx              👈 主页（最重要）
  ├── store.tsx             👈 数据状态管理
  ├── layout.tsx            👈 页面布局
  └── globals.css           👈 全局样式

components/
  ├── ExhibitionMap.tsx     👈 3D 地图组件
  ├── ImmersiveExperience.tsx 👈 沉浸模式
  ├── CulturalAssessment.tsx 👈 文化测评
  └── CaseLibrarySearch.tsx 👈 案例库搜索
```

### 编辑建议

- **修改样式？** 编辑 `app/globals.css` 或 `tailwind.config.js`
- **改变数据？** 编辑 `app/store.tsx` 中的 `exhibitions` 数组
- **新增组件？** 在 `components/` 目录创建新文件
- **改变颜色？** 编辑 `tailwind.config.js` 的 theme

---

## 🔧 配置文件

### TypeScript 配置 (`tsconfig.json`)
- 启用严格模式
- 配置路径别名 `@/*`

### Tailwind CSS (`tailwind.config.js`)
- 深色主题配置
- 自定义动画
- 扩展颜色系统

### Next.js 配置 (`next.config.js`)
- 优化和性能设置
- 静态资源配置

---

## 🎨 自定义指南

### 改变展厅区域

编辑 `app/store.tsx` 中的 `exhibitions` 数组：

```typescript
{
  id: 'your-id',
  name: '您的展区名称',
  stage: 1,
  description: '展区描述',
  highlights: ['要点1', '要点2', '要点3'],
  position: [0, 0, 0], // x, y, z 坐标
}
```

### 改变颜色主题

编辑 `app/globals.css`：

```css
/* 改变主色调 */
--primary-color: #your-color;
```

或编辑 `tailwind.config.js`：

```javascript
theme: {
  extend: {
    colors: {
      primary: '#your-color',
    }
  }
}
```

### 添加音频/视频

编辑 `app/store.tsx` 中的展区配置：

```typescript
{
  // ...
  audioUrl: '/audio/your-audio.mp3',
  videoUrl: '/videos/your-video.mp4',
}
```

---

## 🐛 问题排查

### 问题：端口 3000 已被占用
```bash
# 使用不同的端口
npm run dev -- -p 3001
```

### 问题：依赖安装失败
```bash
# 清除缓存后重新安装
rm -rf node_modules package-lock.json
npm install
```

### 问题：3D 地图不显示
- 检查浏览器是否支持 WebGL
- 尝试更换浏览器（推荐 Chrome）
- 查看浏览器控制台错误信息

---

## 📚 更多资源

- [完整文档](./README.md)
- [贡献指南](./CONTRIBUTING.md)
- [项目 Issues](https://github.com/rzl021002-stack/HaierCulture/issues)

---

## ❓ 需要帮助？

1. 查看 README.md 的常见问题部分
2. 搜索 GitHub Issues
3. 提交新 Issue 描述您的问题

---

**祝您开发愉快！** 🎉
